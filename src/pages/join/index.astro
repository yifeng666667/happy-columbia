---
import Base from '../../layouts/Base.astro';
---

<Base title="一起做这件事">

  <a href="/" class="inline-flex items-center gap-1.5 text-stone-400 hover:text-columbia-navy text-xs transition-colors no-underline mb-8 group">
    <span class="group-hover:-translate-x-0.5 transition-transform inline-block">←</span>
    返回首页
  </a>

  <header class="mb-14 pb-10 border-b border-stone-200">
    <p class="text-[11px] uppercase tracking-[0.25em] text-stone-400 mb-3">Join Us</p>
    <h1 class="font-display font-bold text-3xl md:text-4xl text-stone-900 leading-tight">
      一起做这件事
    </h1>
    <p class="mt-5 text-stone-600 leading-loose text-[0.9375rem] max-w-2xl">
      SIPA 两年，收获了很多——遇见了好的老师与朋友，也经历了许多感动与成长。
      快毕业了，总觉得这些东西不该只留在自己的笔记本里。
      如果能有更多人一起来做这件事，把各自走过的路记录下来，那就更好了。
    </p>
    <a href="mailto:yh3811@columbia.edu"
       class="inline-flex items-center gap-2.5 mt-6 bg-white border border-stone-200
              rounded-full px-4 py-2 no-underline hover:border-columbia-blue
              hover:shadow-sm transition-all duration-200 group">
      <span class="w-2 h-2 rounded-full bg-columbia-blue shrink-0"></span>
      <span class="font-mono text-sm text-stone-700 group-hover:text-columbia-navy transition-colors">
        yh3811@columbia.edu
      </span>
      <span class="text-stone-300 group-hover:text-columbia-blue text-xs transition-colors">↗</span>
    </a>
  </header>

  <!-- What we welcome -->
  <section class="mb-14">
    <p class="text-[11px] uppercase tracking-[0.25em] text-stone-400 mb-6">欢迎什么样的分享</p>
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
      {[
        {
          title: '课程笔记',
          body: '你上过的任何一门 SIPA 课程（或任何你认为值得分享的好课）。不需要完美、不需要系统——哪怕只是几页 key takeaways、一张课程框架图、一份 reading list 的读后感，都欢迎。',
          dot: 'bg-violet-500',
        },
        {
          title: '求职经验',
          body: '从暑期实习到 return offer，从投行、咨询到联合国、NGO、智库——SIPA 人的职业路径极其多元。你走过的路，可能正是另一个人正在摸索的方向。',
          dot: 'bg-emerald-500',
        },
        {
          title: '求学心得',
          body: '选课策略、时间管理、如何和教授建立关系、纽约生活、英语学习……任何你觉得"当年要是有人告诉我就好了"的事。',
          dot: 'bg-amber-500',
        },
        {
          title: '翻译 · 校对 · 排版',
          body: '如果你不写原创内容，但愿意帮忙把现有笔记翻译成英文、校对错字、优化排版——同样重要，同样欢迎。',
          dot: 'bg-columbia-blue',
        },
      ].map(({ title, body, dot }) => (
        <div class="bg-white border border-stone-200 rounded-xl p-6 space-y-2">
          <div class="flex items-center gap-2.5">
            <span class={`w-2 h-2 rounded-full shrink-0 ${dot}`}></span>
            <h3 class="font-semibold text-stone-900">{title}</h3>
          </div>
          <p class="text-sm text-stone-600 leading-relaxed">{body}</p>
        </div>
      ))}
    </div>
  </section>

  <!-- Commitments -->
  <section class="mb-14">
    <p class="text-[11px] uppercase tracking-[0.25em] text-stone-400 mb-6">我的承诺</p>
    <div class="bg-columbia-light/40 border border-columbia-blue/20 rounded-2xl p-6 sm:p-8 space-y-4">
      {[
        { label: '完全公益', body: '所有内容免费开放，不设付费墙，不接广告，永远。' },
        { label: '署名权', body: '贡献者保留署名权，也可选择匿名发布。' },
        { label: '版权尊重', body: '只收录原创或合法授权的内容，不转载受版权保护的讲义原文。' },
        { label: '随时可撤回', body: '你贡献的内容，任何时候想撤下都可以，一句话的事。' },
      ].map(({ label, body }) => (
        <div class="flex gap-4 items-start">
          <span class="mt-0.5 w-1.5 h-1.5 rounded-full bg-columbia-blue shrink-0 mt-2"></span>
          <p class="text-sm text-stone-700 leading-relaxed">
            <strong class="text-columbia-navy font-semibold">{label}</strong>
            <span class="text-stone-500">：</span>
            {body}
          </p>
        </div>
      ))}
    </div>
  </section>

  <!-- Roadmap -->
  <section class="mb-14">
    <p class="text-[11px] uppercase tracking-[0.25em] text-stone-400 mb-2">Roadmap</p>
    <h2 class="font-display font-bold text-xl text-stone-900 mb-2">接下来想做的事</h2>
    <p class="text-stone-500 text-sm leading-relaxed mb-8 max-w-2xl">
      这个网站刚起步，很多想法还在脑子里。把它写出来，也是一种承诺。
    </p>

    <!-- Phase 1: Now -->
    <div class="mb-8">
      <div class="flex items-center gap-3 mb-4">
        <span class="inline-flex items-center gap-1.5 text-[10px] font-semibold uppercase tracking-[0.2em] bg-emerald-50 text-emerald-600 border border-emerald-200 px-3 py-1 rounded-full">
          <span class="w-1.5 h-1.5 rounded-full bg-emerald-500 animate-pulse"></span>
          进行中
        </span>
      </div>
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
        {[
          { icon: '📒', title: '课程笔记库', body: '持续更新 SIPA 核心课程笔记，目前已上线 8 门课，计划覆盖更多方向。' },
          { icon: '🧭', title: '求职系列文章', body: '从「找到热爱的方向」到简历、面试、选公司，逐章更新中。' },
          {
            icon: '🎙️',
            title: '100位校友采访计划',
            body: '采访来自不同行业的在职 SIPA 校友——投行、买方、卖方、咨询、公共部门、创业……真实的职业路径，真实的感受，发布在这里。',
          },
          {
            icon: '🎧',
            title: '小宇宙播客',
            body: '聊求职、聊求学，也聊生活里那些说不清楚的迷茫。不做干货节目，只聊真实的人和真实的事。',
          },
        ].map(({ icon, title, body }) => (
          <div class="bg-white border border-stone-200 rounded-xl p-5 flex gap-4">
            <span class="text-xl shrink-0 mt-0.5">{icon}</span>
            <div>
              <p class="font-semibold text-stone-900 text-sm mb-1">{title}</p>
              <p class="text-stone-500 text-xs leading-relaxed">{body}</p>
            </div>
          </div>
        ))}
      </div>
    </div>

    <!-- Phase 3: Future -->
    <div>
      <div class="flex items-center gap-3 mb-4">
        <span class="inline-flex items-center gap-1.5 text-[10px] font-semibold uppercase tracking-[0.2em] bg-stone-100 text-stone-500 border border-stone-200 px-3 py-1 rounded-full">
          <span class="w-1.5 h-1.5 rounded-full bg-stone-400"></span>
          未来计划中
        </span>
      </div>
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
        {[
          {
            icon: '🏙️',
            title: '城市线下聚会',
            body: '纽约、北京、上海、香港——小型桌游局、校友见面、非正式论坛。不做大型活动，只做真实的连接。',
          },
          {
            icon: '🗺️',
            title: '校友职业地图',
            body: '可视化 SIPA 毕业生的去向分布：哪个城市、哪个行业、哪家公司——让「SIPA 能去哪」有一个真实的答案。',
          },
          {
            icon: '⭐',
            title: '选课众包指南',
            body: '每门课的真实评价：教授风格、workload、考核方式、性价比——从校友视角重写一份选课手册。',
          },
          {
            icon: '💬',
            title: '校友问答专栏',
            body: '在职校友在线答疑。你有问题，有人真的走过那条路，一问一答，发布在这里。',
          },
        ].map(({ icon, title, body }) => (
          <div class="bg-stone-50 border border-stone-200 rounded-xl p-5 flex gap-4">
            <span class="text-xl shrink-0 mt-0.5">{icon}</span>
            <div>
              <p class="font-semibold text-stone-700 text-sm mb-1">{title}</p>
              <p class="text-stone-400 text-xs leading-relaxed">{body}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Contact -->
  <section>
    <p class="text-[11px] uppercase tracking-[0.25em] text-stone-400 mb-6">怎么开始</p>
    <p class="text-stone-600 leading-loose text-[0.9375rem] mb-8 max-w-2xl">
      最简单的方式就是发一封邮件或加个微信，先聊两句你想分享什么。
      不需要正式的"申请"，也不需要先写好完整稿件——
      我们可以一起决定怎么呈现最好。
    </p>

    <div class="bg-white border border-stone-200 rounded-2xl overflow-hidden shadow-sm">
      <div class="px-8 py-7 border-b border-stone-200">
        <p class="text-stone-400 text-[11px] uppercase tracking-[0.2em] mb-1">Contact</p>
        <h3 class="font-display font-bold text-stone-900 text-xl">联系峰哥</h3>
        <p class="mt-1.5 text-stone-500 text-sm">一般 24 小时内回复，欢迎随时打扰。</p>
      </div>
      <div class="grid grid-cols-1 sm:grid-cols-2 divide-y sm:divide-y-0 sm:divide-x divide-stone-200">
        <div class="px-8 py-5">
          <p class="text-stone-400 text-[10px] uppercase tracking-widest mb-1">邮箱</p>
          <a href="mailto:yh3811@columbia.edu"
             class="text-stone-900 hover:text-columbia-navy font-mono text-sm transition-colors no-underline">
            yh3811@columbia.edu
          </a>
        </div>
      </div>
    </div>
  </section>

</Base>
