---
import '../styles/global.css';
interface Props {
  title: string;
  description?: string;
  hero?: boolean;
  ogImage?: string;
  lang?: 'zh' | 'en';
}
const {
  title,
  description = '面向中文读者的经济金融政策知识博客，个人学习整理与原创评论',
  hero = false,
  ogImage = 'https://images.unsplash.com/photo-1486406146926-c627a92ad1ab?auto=format&fit=crop&w=1200&q=80',
  lang = 'zh',
} = Astro.props;

const fullTitle = `${title} · HappyColumbia`;
const siteUrl = 'https://happy-columbia.pages.dev';
---

<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <title>{fullTitle}</title>

    <!-- ── Open Graph ── -->
    <meta property="og:type"        content="website" />
    <meta property="og:site_name"   content="HappyColumbia" />
    <meta property="og:title"       content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:image"       content={ogImage} />
    <meta property="og:image:width"  content="1200" />
    <meta property="og:image:height" content="630" />

    <!-- ── Twitter / X Card ── -->
    <meta name="twitter:card"        content="summary_large_image" />
    <meta name="twitter:title"       content={fullTitle} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image"       content={ogImage} />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link rel="preconnect" href="https://images.unsplash.com" />
    <!-- KaTeX math rendering -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.11/dist/katex.min.css" crossorigin="anonymous" />
  </head>

  <body class={`min-h-screen antialiased font-sans text-stone-900 ${hero ? 'bg-[#061828]' : 'bg-stone-50'}`}>

    <!-- ── Header ─────────────────────────────────────── -->
    <header
      id="site-header"
      data-hero={hero ? "true" : "false"}
      class="top-0 z-50 w-full transition-all duration-300 border-b site-header"
    >
      <div class="max-w-6xl mx-auto px-4 sm:px-5 py-3 sm:py-4 flex items-center justify-between gap-4">

        <!-- Logo -->
        <a href="/" class="no-underline leading-tight group shrink-0">
          <div class="logo-main font-display font-semibold tracking-tight text-lg transition-colors">
            HappyColumbia
          </div>
          <div class="logo-sub block text-[13px] tracking-widest uppercase mt-0.5">
            经济 · 金融 · 职场
          </div>
        </a>

        <!-- Desktop Nav -->
        <nav class="hidden md:flex items-center gap-1 text-sm shrink-0">
          {lang === 'zh' ? [
            { href: '/about-sipa/', label: '关于 SIPA' },
            { href: '/#courses', label: '专题笔记' },
            { href: '/career/', label: '求职分享' },
            { href: '/industry/', label: '行业解析' },
            { href: '/research/', label: '金融投研' },
            { href: '/alumni/', label: 'SIPA校友圈' },
            { href: '/join/',    label: '一起做这件事' },
          ] : [
            { href: '/en/', label: 'Courses' },
            { href: '/about-sipa/', label: 'About SIPA' },
            { href: '/join/', label: 'Contact' },
          ].map(({ href, label }) => (
            <a href={href} class="nav-link px-3 py-1.5 rounded-md text-xs sm:text-sm transition-colors no-underline">
              {label}
            </a>
          ))}
          <!-- Language toggle -->
          <button
            id="lang-toggle"
            data-lang={lang}
            title={lang === 'en' ? '切换到中文' : 'Switch to English'}
            class="lang-toggle nav-link ml-1 flex items-center gap-1 px-3 py-1.5 rounded-md text-xs sm:text-sm font-medium border transition-all cursor-pointer"
          >
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" class="w-3.5 h-3.5 shrink-0">
              <path d="M7.75 2.75a.75.75 0 00-1.5 0v1.258a32.987 32.987 0 00-3.599.278.75.75 0 10.198 1.487A31.545 31.545 0 018.7 5.545 19.381 19.381 0 017 9.56a19.418 19.418 0 01-1.002-1.05.75.75 0 00-1.094 1.022 20.901 20.901 0 001.187 1.243 32.486 32.486 0 01-4.236 2.657.75.75 0 10.668 1.341 33.968 33.968 0 004.587-2.888 20.866 20.866 0 003.502 2.388.75.75 0 10.736-1.305 19.383 19.383 0 01-3.095-2.107A20.875 20.875 0 009.235 9.56 20.882 20.882 0 0011 6.44a.75.75 0 00-.75-.75H6.5V2.75z"/>
              <path d="M12.5 2.75a.75.75 0 00-1.5 0V5h-1a.75.75 0 000 1.5h1v1.25A.75.75 0 0012.5 8.5a.75.75 0 00.75-.75V6.5h1a.75.75 0 000-1.5h-1V2.75z"/>
              <path d="M13.25 9.5a.75.75 0 00-.75.75v1.69l-1.68-1.682a.75.75 0 10-1.06 1.061l2.5 2.5a.75.75 0 001.06 0l2.5-2.5a.75.75 0 10-1.06-1.06L13.25 11.94V10.25a.75.75 0 00-.75-.75z"/>
            </svg>
            {lang === 'en' ? '中文' : 'EN'}
          </button>
          <a href="/join/" class="nav-cta ml-1 px-4 py-1.5 rounded-full text-xs sm:text-sm font-medium border transition-all no-underline">
            {lang === 'en' ? 'Contact' : '联系我'}
          </a>
        </nav>

        <!-- Mobile Hamburger -->
        <button id="mobile-menu-btn" aria-label="打开菜单"
          class="md:hidden flex flex-col justify-center items-center w-9 h-9 gap-1.5 rounded-md transition-colors ham-btn">
          <span id="ham-1" class="ham-line block w-5 h-0.5 rounded transition-all duration-300"></span>
          <span id="ham-2" class="ham-line block w-5 h-0.5 rounded transition-all duration-300"></span>
          <span id="ham-3" class="ham-line block w-5 h-0.5 rounded transition-all duration-300"></span>
        </button>
      </div>

      <!-- Reading progress bar (bottom of header) -->
      <div
        id="reading-progress-bar"
        class="absolute bottom-0 left-0 h-[2px] bg-columbia-blue transition-none pointer-events-none opacity-0"
        style="width: 0%"
      ></div>

      <!-- Mobile Menu Drawer -->
      <div id="mobile-menu" class="md:hidden hidden border-t border-stone-200 bg-white shadow-lg">
        <nav class="flex flex-col px-5 py-4 gap-1">
          {lang === 'zh' ? [
            { href: '/about-sipa/', label: '关于 SIPA' },
            { href: '/#courses',   label: '专题笔记' },
            { href: '/career/',    label: '求职分享' },
            { href: '/industry/',  label: '行业解析' },
            { href: '/research/',  label: '金融投研' },
            { href: '/alumni/',    label: 'SIPA校友圈' },
            { href: '/join/',      label: '一起做这件事' },
          ] : [
            { href: '/en/',         label: 'Courses' },
            { href: '/about-sipa/', label: 'About SIPA' },
            { href: '/join/',       label: 'Contact' },
          ].map(({ href, label }) => (
            <a href={href} class="block px-3 py-2.5 rounded-lg text-stone-700 text-sm font-medium hover:bg-stone-100 hover:text-columbia-navy transition-colors no-underline">
              {label}
            </a>
          ))}
          <button
            id="lang-toggle-mobile"
            data-lang={lang}
            class="mt-1 flex items-center gap-2 px-3 py-2.5 rounded-lg text-stone-700 text-sm font-medium hover:bg-stone-100 hover:text-columbia-navy transition-colors cursor-pointer w-full"
          >
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" class="w-4 h-4 shrink-0">
              <path d="M7.75 2.75a.75.75 0 00-1.5 0v1.258a32.987 32.987 0 00-3.599.278.75.75 0 10.198 1.487A31.545 31.545 0 018.7 5.545 19.381 19.381 0 017 9.56a19.418 19.418 0 01-1.002-1.05.75.75 0 00-1.094 1.022 20.901 20.901 0 001.187 1.243 32.486 32.486 0 01-4.236 2.657.75.75 0 10.668 1.341 33.968 33.968 0 004.587-2.888 20.866 20.866 0 003.502 2.388.75.75 0 10.736-1.305 19.383 19.383 0 01-3.095-2.107A20.875 20.875 0 009.235 9.56 20.882 20.882 0 0011 6.44a.75.75 0 00-.75-.75H6.5V2.75z"/>
            </svg>
            {lang === 'en' ? '切换到中文' : 'Switch to English'}
          </button>
          <a href="/join/" class="mt-2 block text-center px-4 py-2.5 rounded-full text-sm font-semibold border border-columbia-navy text-columbia-navy hover:bg-columbia-navy hover:text-white transition-all no-underline">
            {lang === 'en' ? 'Contact' : '联系我'}
          </a>
        </nav>
      </div>
    </header>

    <style>
      /* ── Position ── */
      .site-header                                                     { position: sticky; }
      .site-header[data-hero="true"]                                   { position: absolute; }
      .site-header[data-hero="true"][data-scrolled="true"]             { position: fixed; }

      /* ── Default (non-hero) ── */
      .site-header                          { background: white; border-color: rgb(231,229,228); box-shadow: 0 1px 14px rgba(0,0,0,0.06); }
      .site-header .logo-main               { color: rgb(28,25,23); }
      .site-header .logo-sub                { color: rgb(168,162,158); }
      .site-header .nav-link                { color: rgb(87,83,78); }
      .site-header .nav-link:hover          { background: rgb(245,245,244); color: rgb(28,25,23); }
      .site-header .nav-cta                 { border-color: rgb(214,211,209); color: rgb(68,64,60); }
      .site-header .nav-cta:hover           { border-color: #0B2C56; color: #0B2C56; }
      .site-header .ham-line                { background: rgb(68,64,60); }
      .site-header .ham-btn:hover           { background: rgb(245,245,244); }

      /* ── Hero top ── */
      .site-header[data-hero="true"]                    { background: transparent; border-color: rgba(255,255,255,0.1); }
      .site-header[data-hero="true"] .logo-main         { color: white; font-weight: 700; }
      .site-header[data-hero="true"] .logo-sub          { color: rgba(255,255,255,0.75); }
      .site-header[data-hero="true"] .nav-link          { color: white; font-weight: 600; }
      .site-header[data-hero="true"] .nav-link:hover    { background: rgba(255,255,255,0.15); color: white; }
      .site-header[data-hero="true"] .nav-cta           { border-color: rgba(255,255,255,0.6); color: white; font-weight: 600; }
      .site-header[data-hero="true"] .nav-cta:hover     { background: white; color: #0B2C56; }
      .site-header[data-hero="true"] .ham-line          { background: white; }
      .site-header[data-hero="true"] .ham-btn:hover     { background: rgba(255,255,255,0.1); }

      /* ── Hero scrolled — clean white, dark text ── */
      .site-header[data-hero="true"][data-scrolled="true"]                 { background: #ffffff; border-color: rgb(231,229,228); box-shadow: 0 1px 14px rgba(0,0,0,0.07); }
      .site-header[data-hero="true"][data-scrolled="true"] .logo-main      { color: rgb(28,25,23); font-weight: 700; }
      .site-header[data-hero="true"][data-scrolled="true"] .logo-sub       { color: rgb(168,162,158); }
      .site-header[data-hero="true"][data-scrolled="true"] .nav-link       { color: rgb(68,64,60); font-weight: 500; }
      .site-header[data-hero="true"][data-scrolled="true"] .nav-link:hover { background: rgb(245,245,244); color: rgb(28,25,23); }
      .site-header[data-hero="true"][data-scrolled="true"] .nav-cta        { border-color: rgb(214,211,209); color: rgb(68,64,60); font-weight: 500; }
      .site-header[data-hero="true"][data-scrolled="true"] .nav-cta:hover  { border-color: #0B2C56; color: #0B2C56; }
      .site-header[data-hero="true"][data-scrolled="true"] .ham-line       { background: rgb(68,64,60); }
      .site-header[data-hero="true"][data-scrolled="true"] .ham-btn:hover  { background: rgb(245,245,244); }
    </style>

    <!-- ── Main ───────────────────────────────────────── -->
    <main class={hero ? 'overflow-x-hidden' : 'max-w-5xl mx-auto px-4 sm:px-6 py-10 sm:py-14 overflow-x-hidden'}>
      <slot />
    </main>

    <!-- ── Footer ────────────────────────────────────── -->
    <footer class="bg-columbia-navy text-white/60 mt-0">
      <div class="max-w-6xl mx-auto px-4 sm:px-6 py-10 md:py-14 grid grid-cols-1 md:grid-cols-3 gap-8 md:gap-10">
        <div>
          <p class="font-display font-semibold text-white text-lg">HappyColumbia</p>
          <p class="mt-2 text-sm leading-relaxed">
            经济 · 金融 · 政策<br />个人原创知识博客
          </p>
          <p class="mt-4 text-xs text-white/40 leading-relaxed">
            纯公益，不盈利、不收费、无广告。<br />
            内容为个人学习整理与原创评论，与任何机构无关联。
          </p>
        </div>
        <div>
          <p class="text-white font-medium text-sm uppercase tracking-widest mb-4">导航</p>
          <ul class="space-y-2 text-sm">
            <li><a href="/about-sipa/" class="hover:text-white transition-colors no-underline">关于 SIPA</a></li>
            <li><a href="/#courses" class="hover:text-white transition-colors no-underline">课程笔记</a></li>
            <li><a href="/career/" class="hover:text-white transition-colors no-underline">求职经验分享</a></li>
            <li><a href="/research/" class="hover:text-white transition-colors no-underline">金融投研</a></li>
            <li><a href="/alumni/" class="hover:text-white transition-colors no-underline">SIPA校友圈</a></li>
            <li><a href="/join/"   class="hover:text-white transition-colors no-underline">一起做这件事</a></li>
          </ul>
        </div>
        <div>
          <p class="text-white font-medium text-sm uppercase tracking-widest mb-4">联系</p>
          <ul class="space-y-2 text-sm">
            <li>
              <a href="mailto:yh3811@columbia.edu" class="hover:text-white transition-colors no-underline">
                yh3811@columbia.edu
              </a>
            </li>
          </ul>
        </div>
      </div>
      <div class="border-t border-white/10 max-w-6xl mx-auto px-4 sm:px-6 py-4 sm:py-5 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-2 text-xs text-white/30">
        <span class="shrink-0">© 2025 HappyColumbia</span>
        <span class="leading-relaxed">内容受国际资深教授、著名投资家与银行家启发，由 AI 辅助整理，不代表任何机构立场。若内容涉及版权，请联系删除。</span>
      </div>
    </footer>

    <!-- ── Back to top button ───────────────────────── -->
    <button
      id="back-to-top"
      aria-label="回到顶部"
      class="fixed bottom-6 right-5 z-50 w-10 h-10 rounded-full bg-columbia-navy text-white flex items-center justify-center shadow-lg opacity-0 pointer-events-none transition-all duration-300 hover:bg-columbia-blue hover:scale-110 active:scale-95"
    >
      <svg viewBox="0 0 20 20" fill="currentColor" class="w-4 h-4">
        <path fill-rule="evenodd" d="M10 17a.75.75 0 01-.75-.75V5.612L5.29 9.77a.75.75 0 01-1.08-1.04l5.25-5.5a.75.75 0 011.08 0l5.25 5.5a.75.75 0 11-1.08 1.04L10.75 5.612V16.25A.75.75 0 0110 17z" clip-rule="evenodd"/>
      </svg>
    </button>

  </body>

  <!-- ── Mobile menu toggle ───────────────────────── -->
  <script>
    const btn = document.getElementById('mobile-menu-btn');
    const menu = document.getElementById('mobile-menu');
    const h1 = document.getElementById('ham-1');
    const h2 = document.getElementById('ham-2');
    const h3 = document.getElementById('ham-3');
    let open = false;

    const setOpen = (state: boolean) => {
      open = state;
      menu?.classList.toggle('hidden', !open);
      // Lock body scroll when menu is open so background doesn't scroll
      document.body.style.overflow = open ? 'hidden' : '';
      if (h1 && h2 && h3) {
        h1.style.transform = open ? 'translateY(8px) rotate(45deg)' : '';
        h2.style.opacity   = open ? '0' : '1';
        h3.style.transform = open ? 'translateY(-8px) rotate(-45deg)' : '';
      }
    };

    btn?.addEventListener('click', () => setOpen(!open));

    menu?.querySelectorAll('a').forEach(a => a.addEventListener('click', () => {
      setOpen(false);
    }));

    // Close menu on resize to desktop breakpoint
    window.addEventListener('resize', () => {
      if (window.innerWidth >= 768 && open) setOpen(false);
    });
  </script>

  <!-- ── Header scroll effect ───────────────────────── -->
  <script>
    const header = document.getElementById('site-header');
    if (header) {
      const update = () => {
        header.dataset.scrolled = window.scrollY > 60 ? 'true' : '';
        if (!header.dataset.scrolled) delete header.dataset.scrolled;
      };
      window.addEventListener('scroll', update, { passive: true });
      update();
    }
  </script>

  <!-- ── Reading progress bar ─────────────────────── -->
  <script>
    const bar = document.getElementById('reading-progress-bar');
    if (bar) {
      const isHomePage = window.location.pathname === '/';
      const isLongPage = document.body.scrollHeight > window.innerHeight * 1.8;
      if (!isHomePage && isLongPage) {
        bar.style.opacity = '1';
        const update = () => {
          const scrollable = document.body.scrollHeight - window.innerHeight;
          const pct = scrollable > 0 ? Math.min(100, (window.scrollY / scrollable) * 100) : 0;
          bar.style.width = pct + '%';
        };
        window.addEventListener('scroll', update, { passive: true });
        update();
      }
    }
  </script>

  <!-- ── Back to top ──────────────────────────────── -->
  <script>
    const topBtn = document.getElementById('back-to-top');
    if (topBtn) {
      window.addEventListener('scroll', () => {
        const show = window.scrollY > 400;
        topBtn.style.opacity = show ? '1' : '0';
        topBtn.style.pointerEvents = show ? 'auto' : 'none';
        topBtn.style.transform = show ? 'translateY(0)' : 'translateY(8px)';
      }, { passive: true });
      topBtn.addEventListener('click', () => window.scrollTo({ top: 0, behavior: 'smooth' }));
    }
  </script>

  <!-- ── Language toggle ─────────────────────────── -->
  <script>
    function handleLangToggle(btn: HTMLElement | null) {
      if (!btn) return;
      btn.addEventListener('click', () => {
        const path = window.location.pathname;
        let target: string;
        if (path.startsWith('/en/courses/')) {
          target = path.replace('/en/courses/', '/courses/');
        } else if (path.startsWith('/courses/')) {
          target = path.replace('/courses/', '/en/courses/');
        } else if (path.startsWith('/en')) {
          target = path.replace(/^\/en/, '') || '/';
        } else {
          target = '/en/';
        }
        window.location.href = target;
      });
    }
    handleLangToggle(document.getElementById('lang-toggle'));
    handleLangToggle(document.getElementById('lang-toggle-mobile'));
  </script>

  <!-- ── TOC scroll spy (anchors) ─────────────────── -->
  <script>
    (() => {
      // Find all TOC links pointing to #anchors
      const tocLinks = Array.from(
        document.querySelectorAll<HTMLAnchorElement>('nav a[href^="#"]')
      );
      if (tocLinks.length === 0) return;

      const sections = tocLinks
        .map(l => document.getElementById(l.getAttribute('href')!.slice(1)))
        .filter(Boolean) as HTMLElement[];

      if (sections.length === 0) return;

      const activate = (id: string) => {
        tocLinks.forEach(l => {
          const active = l.getAttribute('href') === `#${id}`;
          l.style.color = active ? '#0B2C56' : '';
          l.style.fontWeight = active ? '600' : '';
        });
      };

      window.addEventListener('scroll', () => {
        let current = sections[0].id;
        for (const s of sections) {
          if (window.scrollY >= s.offsetTop - 140) current = s.id;
        }
        activate(current);
      }, { passive: true });
    })();
  </script>

</html>
